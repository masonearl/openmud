# Environment variables for Rockmud
#
# Copy this file to .env.local for local dev, or add to:
# Vercel → Project → Settings → Environment Variables
#
# Never commit real keys.

# Required: LLM API keys (at least one)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Override the predict/feedback proxy endpoint.
# Defaults to the public rockmud.com endpoint if not set.
# Set this if you're running your own contech API backend.
# CONTECH_API_URL=https://your-api.example.com/api/contech
