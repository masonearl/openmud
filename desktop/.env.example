# openmud local desktop env (optional)
# Copy to desktop/.env when running desktop-only workflows.
#
# Chat requires at least one:
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Comma-separated sender accounts shown in desktop email actions.
OPENMUD_EMAIL_ACCOUNTS=you@company.com
